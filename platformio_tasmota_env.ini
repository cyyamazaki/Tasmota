[env]
platform                    = ${common.platform}
platform_packages           = ${common.platform_packages}
framework                   = ${common.framework}
board                       = ${common.board}
board_build.filesystem      = ${common.board_build.filesystem}
build_unflags               = ${common.build_unflags}
build_flags                 = ${esp82xx_defaults.build_flags}
monitor_speed               = ${common.monitor_speed}
monitor_echo                = ${common.monitor_echo}
upload_resetmethod          = ${common.upload_resetmethod}
extra_scripts               = ${esp_defaults.extra_scripts}
lib_ldf_mode                = ${common.lib_ldf_mode}
lib_compat_mode             = ${common.lib_compat_mode}
lib_extra_dirs              = ${common.lib_extra_dirs}
lib_ignore                  = ${esp82xx_defaults.lib_ignore}
; Add files to Filesystem for all env (global). Remove no files entry and add a line with the file to include
custom_files_upload         = no_files

[env:tasmota-1M-essential]
; ESP8266 1M build restricted to relay/switch control plus the most common
; environmental sensors (DHT, DS18x20, HTU21/SI7021, BMP/BME280).
board                   = esp8266_1M
build_flags             = ${env.build_flags}
                          -DFIRMWARE_ESSENTIAL
                          -DCODE_IMAGE_STR='"essential"'
                          -DOTA_URL='"http://ota.tasmota.com/tasmota/release/tasmota-lite.bin.gz"'
lib_extra_dirs          = lib/lib_basic, lib/lib_i2c
